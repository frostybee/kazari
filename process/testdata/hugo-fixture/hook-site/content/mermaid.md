---
title: "Mermaid Through the Hook"
---

A mermaid fence still gets wrapped as a plain language-mermaid block by
the hook (no special casing in the template) and must still be skipped by
the processor.

```mermaid
graph TD
  A --> B
```
